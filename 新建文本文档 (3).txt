#include<stdio.h> 
#include<string.h>
int main(void)
{
	char a[]="2*2/4*1/1*2*3/2";
	int sum=a[0]-'0';
	for(int i=1;i<strlen(a);i+=2)
	{
		if(a[i]=='*')
		{
			sum=sum*(a[i+1]-'0');
		}
		else if(a[i]=='/')
		{
			sum=sum/(a[i+1]-'0');
		}
	}
	printf("%d",sum);
	return 0;
}